name = "elevatorbud-admin-prod"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]
main = "@tanstack/react-start/server-entry"
workers_dev = true

# TODO: Add custom domain when hisskompetens.se is set up in Cloudflare
# routes = [
#   { pattern = "admin.hisskompetens.se", custom_domain = true },
# ]

[secrets]
required = ["DATABASE_URL", "CLERK_SECRET_KEY", "CLERK_WEBHOOK_SECRET"]

[env.dev]
name = "elevatorbud-admin-dev"
workers_dev = true
routes = []
