# Ralph Progress Log
Started: tors  2 apr. 2026 14:51:49 CEST

## Codebase Patterns
- Root tsconfig.json is a base config — individual packages extend it with their own tsconfig.json
- Convex aggregation queries: `.collect()` + JS reduce/filter — no native aggregation functions, use `.withIndex()` for org-scoped queries when possible
- Turbo pipelines: build, dev, lint, typecheck — use `pnpm turbo run <task>` to run across all packages
- pnpm workspaces: apps/* and packages/* — all new apps go in apps/, shared code in packages/
- Package versions: turbo 2.9.3, typescript 6.0.2 (as of 2026-04-02)
- .gitignore covers: node_modules, dist, .turbo, .vinxi, .output, .wrangler, .env, .env.local, *.tsbuildinfo, routeTree.gen.ts
- TanStack Start apps use vite.config.ts with `@tanstack/react-start/plugin/vite` + `@cloudflare/vite-plugin` + `@vitejs/plugin-react`
- App tsconfig.json must NOT set `rootDir: "src"` when `include` also contains `vite.config.ts` — use just `include: ["src", "vite.config.ts"]`
- Route tree files (routeTree.gen.ts) are auto-generated by the TanStack Start Vite plugin during build/dev — do not create manually
- For Cloudflare Workers: use `@cloudflare/vite-plugin` (not Nitro) — this is the recommended approach per TanStack Start docs
- App ports: admin=3001, client=3002, landing=3000
- Native build dependencies (esbuild, sharp, workerd) must be approved in root package.json `pnpm.onlyBuiltDependencies`
- Shared UI package: `@elevatorbud/ui` in packages/ui/ — import components via `@elevatorbud/ui/components/ui/button` etc.
- Tailwind CSS v4 uses CSS-based config (`@import "tailwindcss"`, `@theme inline`) — NO tailwind.config.js
- All apps include `@tailwindcss/vite` in vite.config.ts plugins array
- App CSS entry: `src/styles/app.css` imports `@elevatorbud/ui/styles/globals.css` — loaded via `?url` import in `__root.tsx`
- shadcn/ui components installed via `npx shadcn@latest add <component>` from packages/ui/ directory
- components.json exists in both packages/ui/ (for adding components) and each app (for CLI compatibility)
- `cn()` utility: import from `@elevatorbud/ui/lib/utils` — uses clsx + tailwind-merge
- Stub packages pattern: package.json (name, exports, typecheck script), tsconfig.json (extends root, outDir: dist), src/index.ts
- Package exports for non-UI packages: `".": "./src/index.ts"` and `"./*": "./src/*.ts"` for subpath imports
- Convex 1.34.1 installed as root devDependency — convex/ directory lives at monorepo root, NOT inside an app
- convex.json at root configures Convex (default functions dir is `convex/`)
- convex/tsconfig.json is standalone (does NOT extend root) — typechecked separately via `tsc --noEmit -p convex/tsconfig.json`
- Root `typecheck` script chains: turbo typecheck (all packages) + tsc for convex/
- Turbo root tasks use `//#taskname` syntax in turbo.json — `dev:convex` runs `npx convex dev` alongside app dev servers
- .gitignore includes `convex/_generated` — Convex generates this directory during dev/deploy
- Convex auth helpers in convex/auth.ts: getCurrentUser(ctx), requireAdmin(ctx), requireTenantAccess(ctx, orgId)
- forslagsvarden categories map 1:1 to hissar field names — KATEGORI_TILL_FALT lookup in convex/forslagsvarden.ts
- forslagsvarden.list is accessible to all authenticated users (not admin-only) — needed by combobox components in both admin and client apps
- Convex webhook in convex/http.ts: Clerk user sync via svix-verified POST to /clerk-users-webhook
- Internal mutations for Clerk sync in convex/users.ts: upsertFromClerk, deleteFromClerk
- convex/auth.config.ts configures Clerk as auth provider via CLERK_JWT_ISSUER_DOMAIN env var
- convex/_generated/ stubs needed for local typecheck when `npx convex dev` hasn't been run — create dataModel.ts, server.ts, api.ts matching codegen templates
- convex/tsconfig.json needs `"types": ["node"]` for process.env access in auth.config.ts and http.ts
- Dependencies for Clerk webhook: svix (webhook verification), @clerk/backend (types only)
- @clerk/tanstack-react-start v1.0.8 is the Clerk SDK for TanStack Start — uses `ClerkProvider` (NOT from @clerk/react)
- Clerk v6 removed `SignedIn`/`SignedOut` components — use `Show` from @clerk/react or conditional rendering with `useAuth()`
- ConvexProviderWithClerk comes from `convex/react-clerk` — wraps children with Convex client authenticated via Clerk
- Provider nesting: ClerkProvider > ConvexProviderWithClerk > app content — ClerkProvider must be outermost
- Auth package exports: `@elevatorbud/auth` (components/hooks), `@elevatorbud/auth/provider` (ConvexClerkProvider), `@elevatorbud/auth/server` (clerkMiddleware)
- Packages with JSX need `"jsx": "react-jsx"` in their tsconfig.json compilerOptions
- @clerk/shared may need approval in pnpm.onlyBuiltDependencies if its postinstall becomes blocking
- Apps use Vite alias `@convex` → `../../convex` in vite.config.ts for importing Convex API at runtime
- Apps do NOT add `@convex/*` to tsconfig paths (causes rootDir/include errors) — use `.d.ts` module declarations instead
- Each app has `src/convex-api.d.ts` declaring `@convex/_generated/api` module types — must be updated when new Convex functions are added
- convex/users.ts exports `me` query (public) — apps call `useQuery(api.users.me)` to get current user
- TanStack Router `_authenticated` layout route pattern: pathless layout in `routes/_authenticated.tsx`, child routes in `routes/_authenticated/`
- Route tree files (routeTree.gen.ts) must be regenerated when routes change — update manually for typecheck if not running dev server
- Route protection: `_authenticated.tsx` uses `useAuth()` for Clerk auth check + `useQuery(api.users.me)` for role check
- Admin app requires `roll === "admin"`, client app requires `roll === "kund"` — enforced in `_authenticated.tsx` layout
- `<Navigate to="/login" />` from `@tanstack/react-router` for declarative redirects in route components
- Env vars needed per app: `VITE_CONVEX_URL` (Convex deployment URL), `VITE_CLERK_PUBLISHABLE_KEY` (auto-read by ClerkProvider)
- Sidebar layout: shadcn/ui sidebar component system (SidebarProvider > Sidebar + SidebarInset) — install via `npx shadcn@latest add sidebar` from packages/ui/
- Sidebar collapses to icon-only on desktop (collapsible="icon"), becomes sheet on mobile — responsive by default
- Org selector context: `OrgProvider` + `useSelectedOrg()` in admin app's `src/lib/org-context.tsx` — persists to localStorage
- Admin route stubs: dot notation for nested admin routes (e.g., `admin.organisationer.tsx` → `/admin/organisationer`)
- When adding new Convex modules: update `convex/_generated/api.ts` (add import + module entry) AND `apps/*/src/convex-api.d.ts` (add type declarations)
- `lucide-react` must be installed in any app that directly imports icons (it's a dep of `@elevatorbud/ui` but not transitively available)
- Index route (`/_authenticated/`) shows the Daily Overview (field app home page) — "Ny hiss", "Dagens hissar", "Sök befintlig hiss"
- `hissar.dagensHissar` query: pass `todayStart` timestamp from client for timezone-aware date filtering
- Client app sidebar mirrors admin sidebar structure but WITHOUT admin nav section — only Dashboard, Register, Modernisering, Underhåll
- Client app shows org name as static text in header (via `organisationer.get` query) — no org selector dropdown
- `organisationer.get` query uses `requireTenantAccess` (not `requireAdmin`) — accessible to both admins and customers with access
- `@convex/_generated/dataModel` module declaration needed in convex-api.d.ts for `Id<>` type imports in app components
- Convex mutations use `mutation` from `_generated/server`, queries use `query` — both require args schema via `v` validators
- Convex `db.patch(id, fields)` for partial updates, `db.insert(table, data)` for creates, `db.get(id)` for single doc reads
- For counting related records in Convex: query with index + `.collect()` + `.length` (no native COUNT)
- TanStack Table v8: `useReactTable` + `getCoreRowModel()` + `flexRender()` — use `createColumnHelper<T>()` for typed column defs
- TanStack Form v1: `useForm` + `form.Field` render prop + `form.Subscribe` for submit/canSubmit state
- Convex `useMutation(api.x.y)` returns a callable function — `await mutate(args)` directly
- Edit dialogs: use `key={id}` on inner component to reset TanStack Form state when switching items
- shadcn/ui available components: button, input, card, separator, dropdown-menu, select, sheet, tooltip, skeleton, avatar, scroll-area, collapsible, sidebar, table, dialog, label, badge, switch
- TanStack Form `FormApi` has 11-12 generic params in v1 — don't import it directly. Use a type helper: `function _helper() { return useForm({ defaultValues }); } type FormType = ReturnType<typeof _helper>;`
- Convex `useQuery(api.x.y, "skip")` skips the query entirely — use for conditional queries (e.g., skip uniqueness check when input is empty)
- Switch component from shadcn/ui: `<Switch checked={value} onCheckedChange={(val) => onChange(val)} />` — wrap in border div with min-h-[44px] for touch targets
- Convex actions that call external APIs (Clerk) AND export from a module imported by `_generated/api.ts` cause circular type dependencies — split internal mutations/queries into a separate file (e.g., `anvandareInternal.ts`) and use `anyApi` with local type declarations for internal references
- Clerk Backend SDK: `createClerkClient({ secretKey })` from `@clerk/backend` — access `users.createUser()`, `users.updateUser()`, `users.banUser()`, `users.deleteUser()`
- Convex actions cannot access `ctx.db` directly — use `ctx.runQuery()` / `ctx.runMutation()` with internal functions for database operations
- Admin role check in Convex actions: use an `internalQuery` that calls `requireAdmin(ctx)`, then `ctx.runQuery()` from the action
- `@clerk/backend` v3.2.4: `createUser({ emailAddress: [email], firstName, lastName, skipPasswordRequirement: true })` creates user without password requirement
- Clerk `updateUser(userId, { publicMetadata, firstName, lastName })` syncs metadata — `publicMetadata` stores roll and organisation_id
- Convex `useAction(api.x.y)` from `convex/react` for calling actions (NOT `useMutation`) — actions have `FunctionReference<"action", ...>` type
- TanStack Router `validateSearch` in route definition defines typed search params — all properties must be included when navigating with `search: {}`
- TanStack Form cross-field validation: use `form.Subscribe selector={(state) => state.values.fieldName}` for conditional rendering, `form.getFieldValue("field")` inside validators
- hissar.create auto-adds new category values to forslagsvarden — case-insensitive dedup check before insert
- When adding new Convex modules: update `convex/_generated/api.ts` (gitignored stub) AND both `apps/*/src/convex-api.d.ts` type declarations
- Combobox component: `@elevatorbud/ui/components/ui/combobox` — accepts `suggestions: string[]`, `value`, `onChange`, allows free text + selection from dropdown
- Combobox uses Radix Popover directly (not cmdk/Command) — lightweight, supports keyboard nav and free text entry
- Combobox touch targets: h-11 (44px) for input and min-h-[44px] for dropdown items — meets touch accessibility requirements
- Wizard page at `/ny` in admin app: TanStack Form with HissFormValues type, 9-step navigation with progress indicator, org selector at top
- Wizard form type `HissFormValues` defined in ny.tsx — all fields are strings/booleans (numbers converted at submit time)
- Wizard step content: `StepContent` component receives `step` number — form prop to be threaded when step fields are added
- Form submit type conversion: `toOptionalString(val)` trims + converts empty to undefined, `toOptionalNumber(val)` parses to number or undefined — used when submitting form strings to Convex mutations expecting numbers/optional strings
- Textarea component: `@elevatorbud/ui/components/ui/textarea` — shadcn Textarea with `field-sizing-content` auto-grow
- Review step pattern: define `ReviewSection[]` array mapping form values to labeled fields grouped by wizard step, with edit buttons navigating to corresponding steps
- Form persistence: `apps/admin/src/lib/form-persistence.ts` — saveDraft/loadDraft/clearDraft/hasDraft helpers with localStorage, key format `draft-ny-hiss` (new) or `draft-hiss-{id}` (edit)
- TanStack Form `form.setFieldValue(key, value)` to programmatically restore form state — iterate over draft entries
- Debounced auto-save pattern: `useEffect` watching `form.state.values` + `setTimeout(500ms)` — skip save during draft prompt or after submit success
- `hissar.update` mutation: `db.patch(id, fields)` with all optional fields + auto-set `senast_uppdaterad_av`/`senast_uppdaterad` metadata
- `hissar.search` query: client-side filter on `.collect()` results — Convex has no built-in full-text search, so filter in query handler and limit to 20 results
- Dynamic route params in TanStack Router: `hiss.$id.redigera.tsx` file → `/hiss/:id/redigera` path, access via `Route.useParams()`
- Debounced search pattern: `useState(input)` + `useState(debounced)` + `useEffect` with `setTimeout(300ms)`, pass "skip" to `useQuery` when empty
- `hissar.get` query: fetch single elevator by ID, requires authenticated user — used by edit form
- `checkHissnummer` supports `excludeId` param — pass current hiss ID when editing to avoid false duplicate warnings
- Edit form pattern: single scrollable page with `EditSection` cards (not wizard steps) — more natural for editing existing data
- `serverToFormValues` helper: converts Convex doc to form strings (numbers → String(), undefined → "")
- Diff highlighting: `FieldWrapper` component applies `border-l-4 border-l-amber-500 bg-amber-50/50` when field value differs from original
- `ComboboxField` reusable component: wraps FieldWrapper + form.Field + useSuggestions — reduces repetition for category fields in forms
- Recharts 3.8.1 installed in packages/ui — shared chart components in `packages/ui/src/components/dashboard/`
- Recharts Tooltip `formatter` prop: use `(value) => [String(value), label]` — don't annotate `value` as `number` (type is `ValueType | undefined`)
- Recharts Pie `label` callback: destructure with types `({ name, percent }: { name?: string; percent?: number })` — fields are optional
- Dashboard chart data: single `hissar.chartData` Convex query aggregates all 6 chart datasets in one round trip
- packages/ui wildcard export `"./components/*": "./src/components/*.tsx"` covers nested dirs like `dashboard/charts`
- `hissar.list` query pattern: collect + JS filter/sort/slice for pagination — returns `{ data, totalCount, page, limit, totalPages }`
- Multi-select filter pattern: `v.optional(v.array(v.string()))` args + `new Set()` for O(1) case-insensitive matching
- Org name caching in list queries: `Map<string, string>` cache across page results to minimize db.get calls
- TanStack Table server-side mode: `manualSorting: true` + `manualPagination: true` — table tracks UI state, query does actual sort/page
- Multi-select filter UI: Popover + Checkbox list + Badge count — use for category filters in table views
- `forslagsvarden.list` with `{}` returns all categories — group by `kategori` client-side for filter option lists
- Detail view route: admin uses `hiss.$id.index.tsx` (index sibling to `hiss.$id.redigera.tsx`), client uses `hiss.$id.tsx` (no edit route)
- Detail view pattern: `DetailSection` (Card) + `DetailField` (dt/dd) — handles string, number, boolean, undefined values with Swedish formatting
- Export pattern: `hissar.exportData` query returns all filtered records (no pagination), client-side CSV/Excel generation via `@elevatorbud/utils/export`
- xlsx 0.18.5 installed in packages/utils — `XLSX.utils.aoa_to_sheet()` for array-of-arrays → sheet, `XLSX.write(wb, { type: "array" })` for ArrayBuffer output
- CSV with BOM: prepend `\uFEFF` to CSV string for Excel to correctly detect UTF-8 encoding
- Export UI pattern: Popover with CSV/Excel buttons, lazy-load export data via `useQuery` with "skip" sentinel until user clicks export
- Shared filter args: extract common filter args schema into `filterArgs` const and `fetchAndFilter()` helper in convex/hissar.ts — reused by both `list` and `exportData` queries
- Convex `db.get()` returns union of all table document types — cast to `{ namn: string } | null` when you know the table type
- Apps using Recharts directly must add `recharts` as a direct dependency — it's not transitively available from `@elevatorbud/ui`
- Recharts `ComposedChart` or `LineChart` with `<Bar>` + `<Line>` for combined bar/line charts (e.g., budget per year + cumulative curve)
- For per-bar coloring in Recharts: set `fill` property in data array, render `<Bar>` children with `<rect fill={entry.fill}>` — or use `<Cell>` component
- Navigation from data tables: use `<a href={...}>` instead of typed `<Link>` when route params cause TanStack Router type issues — simpler and avoids `params` type errors
- Modernization urgency color coding: red (≤1 year), orange (2-4 years), yellow (5-9 years), green (10+ years) — based on `currentYear` diff
- Recharts BarChart `onClick` handler: use `(data: any) =>` to avoid TypeScript `MouseHandlerDataParam` type errors — access `data.activePayload[0].payload` for clicked bar data
- Recharts `<Cell>` component inside `<Bar>` for per-bar coloring and opacity — import from `recharts`
- Excel import: `XLSX.utils.sheet_to_json(sheet, { header: 1, raw: false })` for positional string[][] access — `raw: false` forces all values to strings
- Excel import types: `ParsedHiss` includes `_source_row`, `_source_sheet`, `_organisation_namn` metadata for import processing (not stored in DB)
- HISSAR_COLUMNS exported from excel-import.ts — reuse for Rivna hissar parser (same structure minus column AH)

---

## 2026-04-02 - US-001
- Initialized pnpm monorepo with Turborepo
- Files created: package.json, pnpm-workspace.yaml, turbo.json, tsconfig.json, .gitignore, pnpm-lock.yaml
- Created empty apps/ and packages/ directories
- **Learnings for future iterations:**
  - TypeScript 6.0.2 is the current latest stable — pnpm may default to 5.x with `^5.x.x` constraint, so specify `^6.0.2` explicitly
  - Root tsconfig uses composite: true and declaration: true for project references
  - turbo typecheck succeeds with 0 packages (no failures on empty workspace)
  - The root package.json has `packageManager` field set to pnpm@10.30.3
---

## 2026-04-02 - US-002
- Scaffolded three TanStack Start apps: admin (port 3001), client (port 3002), landing (port 3000)
- Each app: package.json, tsconfig.json, vite.config.ts, wrangler.toml, src/router.tsx, src/routes/__root.tsx, src/routes/index.tsx
- Used @cloudflare/vite-plugin (v1.31.0) for Cloudflare Workers deployment instead of Nitro
- Updated root package.json with pnpm.onlyBuiltDependencies for esbuild, sharp, workerd
- Added .wrangler and routeTree.gen.ts to .gitignore
- Files changed: 24 files (3 app directories + root config updates)
- **Learnings for future iterations:**
  - TanStack Start latest (1.167.16) uses Vite 8, React 19.2.4, @cloudflare/vite-plugin 1.31.0
  - Do NOT set `rootDir` in tsconfig when vite.config.ts is included — causes TS6059 error
  - routeTree.gen.ts is generated automatically during `vite build` or `vite dev` by the TanStack Start plugin
  - `@cloudflare/vite-plugin` requires `wrangler` as a peer dependency (^4.80.0)
  - pnpm 10.x requires explicit approval for postinstall scripts via `pnpm.onlyBuiltDependencies` in root package.json
  - `pnpm approve-builds` is interactive — use `pnpm.onlyBuiltDependencies` in package.json for CI/non-interactive
---

## 2026-04-02 - US-003
- Created packages/ui/ with shadcn/ui and Tailwind CSS v4
- Files created: packages/ui/{package.json, tsconfig.json, components.json}, src/lib/utils.ts, src/styles/globals.css, src/components/ui/{button.tsx, input.tsx, card.tsx}
- Added components.json to each app for shadcn CLI compatibility
- Added @tailwindcss/vite plugin to all three apps' vite.config.ts
- Created src/styles/app.css in each app importing shared globals.css
- Updated __root.tsx in each app to load the CSS via ?url import
- Versions: tailwindcss 4.2.2, @tailwindcss/vite 4.2.2, shadcn 4.1.2, radix-ui 1.4.3, tw-animate-css 1.4.0
- **Learnings for future iterations:**
  - shadcn CLI (`npx shadcn@latest add`) works from packages/ui/ — reads components.json for path aliases
  - Latest shadcn uses `radix-ui` unified package (not individual `@radix-ui/*` packages) — e.g. `import { Slot } from "radix-ui"`
  - Tailwind v4 globals.css uses `@import "tailwindcss"`, `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"` (though shadcn CLI didn't add the last one)
  - CSS import chain: app.css → @elevatorbud/ui/styles/globals.css → tailwindcss + tw-animate-css
  - TanStack Start CSS loading: import css file with `?url` suffix, add to `head.links` in route's `head()` function
  - Package exports use `*` wildcard: `"./components/*": "./src/components/*.tsx"` supports nested paths like `ui/button`
---

## 2026-04-02 - US-004
- Created four stub packages: types, auth, utils, email
- Each package: package.json, tsconfig.json, src/index.ts with placeholder export
- Files created: 12 files (3 per package) + pnpm-lock.yaml updated
- All 8 workspace packages typecheck successfully
- **Learnings for future iterations:**
  - Stub packages need minimal setup: package.json + tsconfig.json extending root + src/index.ts
  - pnpm auto-discovers new packages in workspaces on `pnpm install` — no manual registration needed
  - Non-UI packages don't need React peer dependencies or JSX config in tsconfig
  - Export maps: `".": "./src/index.ts"` for main entry, `"./*": "./src/*.ts"` for subpath imports
---

## 2026-04-02 - US-005
- Set up Convex with schema stub and dev server integration
- Files created: convex/schema.ts (empty schema stub), convex/tsconfig.json, convex.json
- Files modified: package.json (added convex devDep, dev:convex script, updated dev/typecheck scripts), turbo.json (added //#dev:convex root task), .gitignore (added convex/_generated), pnpm-lock.yaml
- **Learnings for future iterations:**
  - Convex 1.34.1 is the current latest — uses esbuild 0.27.0 internally
  - convex/ directory has its own tsconfig.json separate from the root — it does NOT extend root tsconfig because Convex needs specific settings
  - Turbo root tasks (`//#taskname`) allow running scripts from root package.json in the turbo pipeline without creating a workspace package
  - `pnpm dev` now runs `turbo run dev dev:convex --concurrency 20` — turbo runs workspace `dev` tasks + root `dev:convex` together
  - `npx convex dev` requires authentication and project setup on first run — the dev script infrastructure is in place but needs initial `npx convex dev` interactive setup
  - Convex generates `_generated/` directory inside convex/ during dev — already added to .gitignore
- Convex schema uses `v.optional()` for nullable fields, `v.union(v.literal(...))` for enums, `v.id("tableName")` for foreign keys
- Schema indexes: chain `.index("name", ["field1", "field2"])` after `defineTable()`
- Most hissar fields are optional (data comes from varied sources: manual entry, Excel import) — only hissnummer, organisation_id, status, skapad_datum are required

---

## 2026-04-02 - US-006
- Defined Convex schema for all four tables: hissar, organisationer, anvandare, forslagsvarden
- hissar table: 35+ fields across 7 sections (identifiering, teknisk spec, dörrar/korg, maskineri, besiktning, modernisering, nödtelefon) + kommentarer + metadata
- organisationer: namn, organisationsnummer, kontaktperson, telefonnummer, email
- anvandare: clerk_user_id, email, namn, roll (admin|kund), organisation_id (optional), aktiv, skapad_datum, senaste_login
- forslagsvarden: kategori, varde, aktiv, skapad_datum
- Indexes: hissar (by_organisation_id, by_hissnummer), anvandare (by_clerk_user_id), forslagsvarden (by_kategori)
- Files changed: convex/schema.ts
- **Learnings for future iterations:**
  - Most hissar fields are optional because data can be incomplete from Excel imports or partial field entry
  - `byggar` is a number (year) but optional — Excel import handles "Okänt" by leaving it undefined
  - `moderniserar` is a string (not number) because it can be "Ej ombyggd" or "2007-vinga" (year + suffix)
  - `marklast`, `korgstorlek`, `dagoppning` stored as strings — compound formats like "500*6" parsed at application level
  - Foreign keys use `v.id("tableName")` — Convex validates these at write time
  - Status uses `v.union(v.literal("aktiv"), v.literal("rivd"), v.literal("arkiverad"))` for strict enum typing
---

## 2026-04-02 - US-007
- Created Convex auth helpers and Clerk webhook integration
- Files created: convex/auth.config.ts, convex/auth.ts, convex/http.ts, convex/users.ts, convex/_generated/{dataModel,server,api}.ts (stubs, gitignored)
- Files modified: convex/tsconfig.json (added "types": ["node"]), package.json (added svix, @clerk/backend, @types/node), pnpm-lock.yaml
- **Learnings for future iterations:**
  - `convex/_generated/` doesn't exist until `npx convex dev` is run (requires auth) — create type stubs matching codegen templates for local typecheck
  - Stubs follow exact patterns from `node_modules/convex/src/cli/codegen_templates/{server,dataModel,api}.ts`
  - dataModel.ts uses "dynamic" variant: imports schema and derives types via `DataModelFromSchemaDefinition<typeof schema>`
  - api.ts must list all modules with Convex function exports (currently just `users.ts`)
  - `process.env` is available in Convex runtime but needs `@types/node` for TypeScript — add `"types": ["node"]` to convex/tsconfig.json
  - Clerk webhook uses svix for verification — requires CLERK_WEBHOOK_SECRET env var
  - auth.config.ts requires CLERK_JWT_ISSUER_DOMAIN env var (set in Convex dashboard)
  - Auth helper pattern: `ctx.auth.getUserIdentity()` returns identity with `subject` = Clerk user ID, then lookup via `by_clerk_user_id` index
  - New users from webhook default to `roll: "kund"` — admin role must be set manually or via admin UI
---

## 2026-04-02 - US-008
- Set up packages/auth with ConvexProviderWithClerk wrapper component
- Files created: packages/auth/src/provider.tsx, packages/auth/src/server.ts
- Files modified: packages/auth/package.json (added @clerk/tanstack-react-start, convex, React peer deps), packages/auth/tsconfig.json (added jsx: react-jsx), packages/auth/src/index.ts (real exports replacing placeholder), pnpm-lock.yaml
- ConvexClerkProvider component wraps ClerkProvider > ConvexProviderWithClerk, takes convexUrl prop
- Re-exports clerkMiddleware from @elevatorbud/auth/server for TanStack Start server middleware
- Re-exports Clerk UI components (SignIn, SignUp, UserButton) and hooks (useAuth, useUser, useSession)
- **Learnings for future iterations:**
  - @clerk/tanstack-react-start v1.0.8 re-exports from @clerk/react v6.1.4 — `SignedIn`/`SignedOut` components no longer exist
  - Use `useAuth()` hook with `isSignedIn` for conditional rendering instead of `SignedIn`/`SignedOut` wrappers
  - `convex/react-clerk` provides `ConvexProviderWithClerk` — it's part of the main `convex` npm package, no separate install needed
  - ClerkProvider from `@clerk/tanstack-react-start` auto-reads `VITE_CLERK_PUBLISHABLE_KEY` env var — no need to pass it as prop
  - ConvexReactClient should be memoized (useMemo) to avoid recreating on re-renders
  - Package exports for TSX files need explicit entries: `"./provider": "./src/provider.tsx"`
  - clerkMiddleware from `@clerk/tanstack-react-start/server` is used in TanStack Start's `createStart()` requestMiddleware
---

## 2026-04-02 - US-009
- Added Clerk login pages to admin and client apps with role-based route protection
- Added `me` query to convex/users.ts so apps can fetch the current authenticated user
- Wrapped both apps with ConvexClerkProvider in __root.tsx
- Created `/login` route in both apps with Clerk `<SignIn />` component
- Created `_authenticated` layout route for route protection (admin: admin role, client: kund role)
- Moved index pages under `_authenticated/` directory
- Added `@convex` Vite alias + `.d.ts` module declarations for Convex API imports
- Files created: convex-api.d.ts (x2), _authenticated.tsx (x2), _authenticated/index.tsx (x2), login.tsx (x2)
- Files modified: convex/users.ts, __root.tsx (x2), vite.config.ts (x2), package.json (x2), pnpm-lock.yaml
- Files deleted: apps/admin/src/routes/index.tsx, apps/client/src/routes/index.tsx
- **Learnings for future iterations:**
  - Cannot add `@convex/*` to tsconfig paths — TypeScript follows the import chain into the convex/ directory causing rootDir/include errors
  - Use Vite `resolve.alias` for runtime resolution + `.d.ts` module declarations for type info — this cleanly separates build from typecheck
  - Each app needs `src/convex-api.d.ts` declaring `@convex/_generated/api` — must be updated when new Convex queries/mutations are added
  - TanStack Router `_authenticated` layout route: delete the old `index.tsx` at route root, create `_authenticated/index.tsx` to avoid path conflict at `/`
  - routeTree.gen.ts must be updated when adding/removing routes — during dev the Vite plugin does this, but for standalone typecheck update manually
  - `useQuery` from `convex/react` returns `undefined` while loading, `null` if no result — handle both states in UI
  - `<Navigate to="/login" />` from `@tanstack/react-router` works for declarative redirects in components
  - Clerk `<SignIn />` with `routing="hash"` and `forceRedirectUrl="/"` handles the sign-in flow and redirect
  - Manual browser verification needed (no browser testing tools configured in this iteration)
---

## 2026-04-02 - US-010
- Implemented admin desktop sidebar layout with org selector
- Installed shadcn/ui sidebar + 10 related components (separator, dropdown-menu, select, sheet, tooltip, skeleton, avatar, scroll-area, collapsible)
- Created AppSidebar component with main nav (Dashboard, Register, Modernisering, Underhåll) and admin nav (Organisationer, Användare, Import, Referensdata)
- Created OrgSelector component with Select dropdown querying organisationer.list
- Created OrgProvider context (persists selected org to localStorage)
- Created convex/organisationer.ts with admin-only `list` query
- Wrapped _authenticated layout with SidebarProvider + SidebarInset structure
- Added header with SidebarTrigger, separator, and OrgSelector
- Created 8 stub route files for navigation destinations (dashboard, register, modernisering, underhall, admin.organisationer, admin.anvandare, admin.import, admin.referensdata)
- Updated _authenticated/index.tsx to redirect `/` → `/dashboard`
- Updated routeTree.gen.ts with all new routes
- Updated convex-api.d.ts with organisationer.list type declaration
- Files created: convex/organisationer.ts, apps/admin/src/{components/app-sidebar.tsx, components/org-selector.tsx, lib/org-context.tsx}, 8 route stubs, 11 shadcn UI components
- Files modified: apps/admin/src/routes/_authenticated.tsx, apps/admin/src/routes/_authenticated/index.tsx, apps/admin/src/convex-api.d.ts, apps/admin/package.json, packages/ui/package.json, packages/ui/src/styles/globals.css, pnpm-lock.yaml
- **Learnings for future iterations:**
  - shadcn/ui sidebar installs many peer components (sheet, tooltip, skeleton, input, separator) — run the add command and it handles deps
  - Sidebar component uses `useIsMobile` hook (installed automatically in packages/ui/src/hooks/use-mobile.ts)
  - Admin app needs `lucide-react` as a direct dependency for icon imports in app-level components
  - Dot notation for route files (`admin.organisationer.tsx`) maps to nested paths (`/admin/organisationer`) in TanStack Router
  - routeTree.gen.ts is gitignored but must be manually updated for typecheck to pass without running dev server
  - convex/_generated/api.ts is gitignored but must include new modules for typecheck — add `import type * as moduleName` and module entry in `ApiFromModules`
  - `useQuery(api.organisationer.list)` returns untyped data when using stub .d.ts — add explicit type annotations in consuming components
  - SidebarProvider wraps TooltipProvider internally — no need to add a separate TooltipProvider
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-011
- Implemented client app sidebar layout with route protection
- Created AppSidebar component with 4 nav items (Dashboard, Register, Modernisering, Underhåll) — no admin section
- Created OrgDisplay component showing org name as static text in header (no dropdown)
- Added `organisationer.get` Convex query with tenant access check (not admin-only)
- Updated _authenticated.tsx layout with SidebarProvider + SidebarInset structure
- Updated index route to redirect `/` → `/dashboard`
- Created 4 route stubs: dashboard.tsx, register.tsx, modernisering.tsx, underhall.tsx
- Updated routeTree.gen.ts with all new routes
- Updated convex-api.d.ts for both admin and client apps (added organisationer.get + dataModel types)
- Added lucide-react as direct dependency to client app
- Files created: apps/client/src/components/{app-sidebar.tsx, org-display.tsx}, 4 route stubs in _authenticated/
- Files modified: apps/client/src/routes/_authenticated.tsx, apps/client/src/routes/_authenticated/index.tsx, apps/client/src/convex-api.d.ts, apps/client/package.json, apps/admin/src/convex-api.d.ts, convex/organisationer.ts, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Client sidebar reuses same shadcn/ui sidebar components as admin — no additional shadcn installs needed
  - `organisationer.get` query needs `requireTenantAccess` (not `requireAdmin`) so customers can see their own org name
  - Client convex-api.d.ts needs `@convex/_generated/dataModel` module declaration for `Id<>` type used in OrgDisplay component
  - Customer user's `organisation_id` comes from the `anvandare` document returned by `api.users.me` — no separate context provider needed
  - Admin app convex-api.d.ts must also be updated when adding new Convex functions to keep types in sync
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-012
- Added Convex mutations and queries for organisationer CRUD
- `organisationer.create` mutation: accepts namn, organisationsnummer, kontaktperson, telefonnummer, email — requires admin role
- `organisationer.update` mutation: accepts id + optional fields, patches existing document — requires admin role
- Enhanced `organisationer.list` to include `antalHissar` count per org (counts via hissar by_organisation_id index)
- `organisationer.get` already existed from US-011 (uses requireTenantAccess, not requireAdmin)
- Updated convex-api.d.ts in both admin and client apps with new mutation type declarations
- Files changed: convex/organisationer.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Convex `db.patch(id, fields)` is the idiomatic way to do partial updates — only updates provided fields
  - For counting related records, query with index + `.collect()` + `.length` — Convex doesn't have native COUNT aggregation
  - `Promise.all()` with `map()` is the pattern for enriching a list with related data in Convex queries
  - Note: `organisationer.get` uses `requireTenantAccess` (not `requireAdmin`) because customers also need to read their own org — the PRD says "all functions require admin role" but get was intentionally different from US-011
---

## 2026-04-02 - US-013
- Implemented admin page for organization management at /admin/organisationer
- TanStack Table with columns: namn, organisationsnummer, kontaktperson, email, antal hissar
- Sortable columns (namn, antal hissar), global search filter
- Create organization dialog with TanStack Form: namn required, organisationsnummer format validation (XXXXXX-XXXX)
- Edit organization dialog opens on row click, pre-fills current values
- Empty state with icon when no organizations found
- Installed shadcn/ui components: table, dialog, label, badge in packages/ui/
- Installed @tanstack/react-table 8.21.3 and @tanstack/react-form 1.28.6 in admin app
- Files created: packages/ui/src/components/ui/{table.tsx, dialog.tsx, label.tsx, badge.tsx}
- Files modified: apps/admin/src/routes/_authenticated/admin.organisationer.tsx, apps/admin/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - TanStack Table v8: `useReactTable` + `getCoreRowModel()` + `flexRender()` — column defs via `createColumnHelper<T>()`
  - TanStack Form v1: `useForm` + `form.Field` render prop + `form.Subscribe` for submit state — `validators.onChange` for sync validation
  - Convex `useMutation` returns a function, not an object — call directly: `const create = useMutation(api.x.create); await create(args)`
  - For edit dialogs with pre-filled form, use a `key={id}` on an inner component to reset form state when switching between items
  - Swedish organisationsnummer format: XXXXXX-XXXX (6 digits, dash, 4 digits) — regex: `/^\d{6}-\d{4}$/`
  - Convert empty strings to `undefined` before passing to Convex mutations (optional fields should not store empty strings)
  - Convex `Id<"table">` type from `@convex/_generated/dataModel` requires casting with `as never` when using string IDs from table data in typed mutation calls
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-014
- Implemented Convex actions and queries for anvandare (user management) with Clerk sync
- Actions: create (Clerk createUser + Convex insert), update (Convex patch + Clerk updateUser), inaktivera (Convex aktiv=false + Clerk banUser), remove (clear hissar refs + Convex delete + Clerk deleteUser)
- Queries: list (filterable by roll, organisation_id, search), get (by ID), listByOrganisation
- All public functions require admin role
- Split into two files to avoid circular type dependency: anvandare.ts (public actions + queries), anvandareInternal.ts (internal mutations + queries)
- Files created: convex/anvandare.ts, convex/anvandareInternal.ts
- Files modified: convex/_generated/api.ts (added anvandare + anvandareInternal modules), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Convex actions that import `internal` from `_generated/api.ts` AND are exported in a module listed in api.ts cause TS2502 circular type errors
  - Solution: put internal mutations/queries in a separate file (e.g., `anvandareInternal.ts`), use `anyApi` from `convex/server` with local type declarations in the actions file
  - `const internalRef = anyApi as unknown as { moduleName: { fnName: FunctionReference<"mutation", "internal"> } }` provides type-safe references without circular imports
  - Clerk `users.createUser({ skipPasswordRequirement: true })` creates a user that can later set their password via Clerk's password reset flow
  - Convex `db.delete(id)` hard-deletes — for referential integrity, clear dangling foreign key references first (skapad_av, senast_uppdaterad_av in hissar)
  - CLERK_SECRET_KEY env var needed in Convex runtime for backend Clerk API calls
---

## 2026-04-02 - US-015
- Implemented admin page for user management at /admin/anvandare
- TanStack Table with columns: namn, email, roll (badge), organisation (name lookup), status (aktiv/inaktiv badge), senaste inloggning
- Filter by roll (Select dropdown: Alla/Admin/Kund) and organisation (Select dropdown from organisationer.list)
- Search by name/email via anvandare.list query search parameter
- Create user dialog with TanStack Form: namn (required), email (required + format validation), roll (Select: admin/kund), organisation (Select, required when roll=kund)
- Quick-action from org edit dialog: "Lägg till kundanvändare" button navigates to /admin/anvandare?org=orgId&create=true
- Auto-opens create dialog with org pre-selected when arriving from org page with ?create=true search param
- Uses `useAction` (not `useMutation`) for anvandare.create since it's a Convex action
- Files modified: apps/admin/src/routes/_authenticated/admin.anvandare.tsx (full implementation), apps/admin/src/routes/_authenticated/admin.organisationer.tsx (added quick-action button + Link import)
- **Learnings for future iterations:**
  - Convex actions use `useAction` from `convex/react` (not `useMutation`) — actions have `FunctionReference<"action", ...>` type
  - TanStack Router `validateSearch` defines search param types — all properties must be included when navigating (use `create: false` when clearing the param)
  - For org name display in user table, fetch `organisationer.list` and build a `Map<id, namn>` lookup — no join queries in Convex
  - `form.Subscribe selector={(state) => state.values.roll}` allows conditional rendering of fields based on other field values in TanStack Form
  - `form.getFieldValue("roll")` can be used inside validators to cross-validate fields
  - Organisation ID from search params needs `as never` cast when passed to Convex query expecting `Id<"organisationer">`
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-016
- Implemented Convex functions for forslagsvarden (suggestion values / reference data)
- `forslagsvarden.list` query: returns values filtered by optional kategori, requires authenticated user (not admin-only)
- `forslagsvarden.create` mutation: creates new value with kategori + varde, sets aktiv=true and skapad_datum, requires admin
- `forslagsvarden.update` mutation: renames value and propagates change to all hissar records using the old value in the corresponding field, requires admin
- `forslagsvarden.merge` mutation: merges source value into target (updates all hissar, deletes source record), validates same kategori, requires admin
- `forslagsvarden.deactivate` mutation: sets aktiv=false (value no longer suggested but existing data preserved), requires admin
- Category-to-field mapping defined for all 11 categories (hisstyp, fabrikat, distrikt, skotselforetag, besiktningsorgan, hissbeteckning, typ_dorrar, kollektiv, drivsystem, maskinplacering, atgarder_vid_modernisering)
- Files created: convex/forslagsvarden.ts
- Files modified: convex/_generated/api.ts (gitignored stub), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Category names in forslagsvarden map 1:1 to field names in the hissar table — use a lookup map for propagating value changes
  - `forslagsvarden.list` is accessible to all authenticated users (not admin-only) since both admin and client apps need suggestion values for combobox fields
  - When updating/merging values, must scan all hissar records since there's no index on each category field — for large datasets this could be slow but acceptable for admin-only operations
  - `forslagsvarden.merge` deletes the source record (hard delete) rather than deactivating — this is intentional since the value has been absorbed into the target
---

## 2026-04-02 - US-017
- Implemented admin page for reference data management at /admin/referensdata
- Category selector dropdown with all 11 categories (hisstyp, fabrikat, distrikt, etc.)
- Card-based list of values per category with active/inactive badge
- Search filter for values within selected category
- Active/inactive count display
- Create value dialog with TanStack Form validation
- Rename dialog (propagates to all elevators via forslagsvarden.update)
- Merge dialog with target value selector and preview card showing the merge direction
- Deactivate button (inline, marks value as inactive)
- Swedish display names for all categories (e.g., "skotselforetag" → "Skötselföretag")
- Files changed: apps/admin/src/routes/_authenticated/admin.referensdata.tsx
- **Learnings for future iterations:**
  - Reference data page uses card-based layout (not TanStack Table) since values are simple key-value pairs with actions
  - The `key={item._id}` pattern on inner dialog components resets TanStack Form state when switching items (same pattern as org edit)
  - forslagsvarden.list query already supports optional `kategori` filter — no backend changes needed for this UI
  - Merge dialog uses manual state management (useState for targetId + isSubmitting) instead of TanStack Form since it's a simple select + submit
  - Category display names mapped via KATEGORI_NAMN constant in the page component
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-018
- Implemented Convex mutation hissar.create with all fields from schema
- Mutation: requires admin role, auto-sets skapad_av + skapad_datum + status='aktiv'
- Validates hissnummer uniqueness via by_hissnummer index (throws Swedish error if duplicate)
- Auto-adds new category values to forslagsvarden table (case-insensitive dedup for all 11 categories)
- Updated convex-api.d.ts in both admin and client apps with hissar.create type declaration
- Updated convex/_generated/api.ts stub with hissar module import
- Files created: convex/hissar.ts
- Files modified: convex/_generated/api.ts (gitignored), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - autoAddForslagsvarden helper scans all existing values per category for case-insensitive dedup — reusable for hissar.update too
  - `requireAdmin(ctx)` returns the admin user document — use `admin._id` for skapad_av instead of a separate getCurrentUser call
  - `ctx.db.query("hissar").withIndex("by_hissnummer", q => q.eq("hissnummer", val)).unique()` returns null if not found — no need for .collect() + .find()
  - convex/_generated/api.ts is gitignored so it's not committed — but it must be updated locally for typecheck to pass
---

## 2026-04-02 - US-019
- Created reusable Combobox component in packages/ui/src/components/ui/combobox.tsx
- Installed shadcn popover component (packages/ui/src/components/ui/popover.tsx) — radix-ui was already a dependency
- Combobox features: input with dropdown popover, filters suggestions as user types, allows free text entry, keyboard navigation (ArrowUp/Down/Enter/Escape), check icon for selected value, "Nytt värde" indicator for new entries
- Touch-optimized: h-11 (44px) input height, min-h-[44px] dropdown items, 44x44px toggle button
- Swedish placeholder "Skriv eller välj..." and messages ("Inga förslag tillgängliga", "Inga matchande förslag", "Nytt värde")
- Component is pure UI — accepts `suggestions: string[]` as prop, no Convex dependency. Auto-adding new values to forslagsvarden happens at the form level (hissar.create mutation already handles this)
- Files created: packages/ui/src/components/ui/combobox.tsx, packages/ui/src/components/ui/popover.tsx
- **Learnings for future iterations:**
  - Radix Popover is lighter than cmdk/Command for combobox that needs free text — cmdk is better when you only want list selection
  - `onOpenAutoFocus={(e) => e.preventDefault()}` on PopoverContent prevents stealing focus from the input when dropdown opens
  - `w-[var(--radix-popover-trigger-width)]` makes the popover match the trigger width automatically
  - Use `onPointerDown` (not `onClick`) for dropdown items to prevent input blur race condition
  - `origin-(--radix-popover-content-transform-origin)` is the Tailwind v4 syntax for Radix animation origins (no `var()` wrapper needed)
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-020
- Implemented elevator wizard shell with step navigation at /ny in admin app
- 9-step wizard: Identifiering, Teknisk specifikation, Dörrar och korg, Maskineri, Besiktning och underhåll, Modernisering, Nödtelefon, Kommentarer, Granska och spara
- TanStack Form initialized with HissFormValues type covering all fields from all 9 steps
- Progress indicator with numbered step buttons (clickable to jump to any step)
- Next/Previous navigation with touch-optimized h-12 buttons (48px)
- Step indicator buttons: min-h-[44px] min-w-[44px] touch targets
- Organisation selector (Select dropdown querying organisationer.list) at top of wizard
- Steps are empty shells showing step number + title placeholder
- Portrait-optimized: vertical flex layout, no horizontal scroll, sticky bottom nav
- Step 9 shows "Spara" button instead of "Nästa" (submit handler to be implemented in US-023)
- Files created: apps/admin/src/routes/_authenticated/ny.tsx
- Files modified: apps/admin/src/routeTree.gen.ts (gitignored, updated locally for typecheck)
- **Learnings for future iterations:**
  - `ReturnType<typeof useForm<HissFormValues>>` doesn't work for typing form props — useForm is a generic function that TypeScript can't infer from typeof
  - For components that will receive the form in future stories, pass it as a prop with explicit type or use React context
  - Wizard route at `/ny` (not `/ny-hiss`) — short path works well for mobile URL bar
  - Step content components receive step number to render the right content — form prop will be added when step content is implemented
  - Sticky bottom nav with `sticky bottom-0 border-t bg-background` keeps buttons visible while scrolling form content
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-021
- Implemented wizard steps 1-3 with full form fields
- Step 1 (Identifiering): hissnummer with real-time uniqueness check via Convex query, adress input, hissbeteckning combobox, distrikt combobox
- Step 2 (Teknisk specifikation): hisstyp combobox, fabrikat combobox, byggår numeric input, hastighet/lyfthöjd side-by-side, marklast compound input, antal plan/dörrar side-by-side
- Step 3 (Dörrar och korg): typ_dorrar combobox, genomgång toggle switch, kollektiv combobox, korgstorlek/dagöppning compound inputs, bärbeslag/dörrmaskin side-by-side
- Added `hissar.checkHissnummer` Convex query for real-time duplicate validation
- Installed shadcn Switch component for toggle fields
- Created `useSuggestions(kategori)` hook wrapping forslagsvarden.list query
- Used type helper pattern for HissForm type (avoids FormApi 11-generic-param issue)
- Files created: packages/ui/src/components/ui/switch.tsx
- Files modified: apps/admin/src/routes/_authenticated/ny.tsx, convex/hissar.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - TanStack Form v1 `FormApi` requires 11-12 generic params — cannot use `FormApi<Data, undefined>`. Instead define `function _helper() { return useForm({defaultValues}); }; type Form = ReturnType<typeof _helper>;`
  - `useQuery(api.x.y, "skip")` from convex/react skips the query when condition not met — useful for conditional validation
  - Step components receive `form` as a prop with the inferred type — field types are properly inferred from form.Field name prop
  - Switch component: wrap in `flex min-h-[44px] items-center justify-between rounded-md border px-3 py-2` for touch-friendly toggle row
  - `useSuggestions` hook filters to `aktiv: true` only — inactive values are not shown in combobox suggestions
  - Side-by-side fields: `grid grid-cols-2 gap-3` for compact layout on mobile
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-022
- Implemented wizard steps 4-6: Maskineri, Besiktning och underhåll, Modernisering
- Step 4 (Maskineri): drivsystem (combobox), upphängning (input), maskinplacering (combobox), typ_maskin + typ_styrsystem (side-by-side inputs)
- Step 5 (Besiktning): besiktningsorgan (combobox), besiktningsmånad (fixed enum Select Jan-Dec), skötselföretag (combobox), schaktbelysning (input)
- Step 6 (Modernisering): moderniseringsår (input), garanti (toggle switch), rekommenderat moderniseringsår (input), budget belopp (numeric input), åtgärder vid modernisering (combobox)
- Files changed: apps/admin/src/routes/_authenticated/ny.tsx (+285 lines)
- **Learnings for future iterations:**
  - Steps 4-6 follow the exact same patterns as steps 1-3 — no new patterns discovered
  - BESIKTNINGSMANADER const array with fixed Swedish month names used for the enum Select (not a combobox, since these are fixed values)
  - Besiktningsmånad uses Select (fixed enum) while other fields use Combobox (free text + suggestions) — the distinction is deliberate per PRD
  - Budget belopp uses `type="number" inputMode="numeric"` for numeric keyboard on mobile
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-023
- Implemented wizard steps 7-9: Nödtelefon, Kommentarer, Granska och spara
- Step 7 (Nödtelefon): har_nodtelefon toggle, modell input, typ input, behöver_uppgradering toggle, pris numeric input
- Step 8 (Kommentarer): fritext Textarea component for free-form comments
- Step 9 (Granska och spara): review page with all entered fields grouped by section (8 sections), edit button per section navigates back to that step
- Submit logic: calls `useMutation(api.hissar.create)` with proper type conversion (strings → numbers for byggar, antal_plan, antal_dorrar, budget_belopp, nodtelefon_pris; empty strings → undefined)
- Offline detection: checks `navigator.onLine` before submit, shows Swedish offline error message with WifiOff icon
- Success confirmation: full-screen success view with "Registrera ny hiss" button that resets form + step
- Loading state: Spara button shows spinner and "Sparar..." text while submitting
- Required field validation on review step: warns if hissnummer or organisation is missing
- Installed shadcn Textarea component in packages/ui/
- Files created: packages/ui/src/components/ui/textarea.tsx
- Files modified: apps/admin/src/routes/_authenticated/ny.tsx (+515 lines, -25 lines)
- **Learnings for future iterations:**
  - `toOptionalString` / `toOptionalNumber` helpers convert form string values to Convex-compatible types (empty string → undefined, string → number)
  - Boolean fields: `value || undefined` converts `false` to `undefined` for optional boolean Convex fields
  - `navigator.onLine` is a quick client-side check but may not catch all offline scenarios — Convex mutation errors with "fetch" in message also indicate connectivity issues
  - Review step uses `form.state.values` to read all current form values reactively
  - `ReviewSection` type pattern: define sections with step number, title, and field array for structured review display
  - Only fields with actual values are shown in review (boolean false = hidden, empty strings = hidden) to keep review concise
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-024
- Implemented form persistence with localStorage drafts for the elevator creation wizard
- Created `apps/admin/src/lib/form-persistence.ts` with helpers: saveDraft, loadDraft, clearDraft, hasDraft, getDraftKey
- Integrated auto-save into NyHiss component with 500ms debounce on form value changes
- Draft restore prompt on page load: "Du har ett sparat utkast. Vill du fortsätta där du slutade?" with Återställ/Börja om buttons
- "Utkast sparat" indicator with Save icon appears in header for 2 seconds after each auto-save
- Draft cleared on successful form submit and on form reset ("Registrera ny hiss")
- Draft also stores currentStep so user resumes at the right wizard step
- Only saves draft when form has actual content (skips empty forms and org-only state)
- Files created: apps/admin/src/lib/form-persistence.ts
- Files modified: apps/admin/src/routes/_authenticated/ny.tsx (+104 lines)
- **Learnings for future iterations:**
  - `form.setFieldValue(key, value as never)` works for programmatic form restoration — need `as never` cast for the generic type
  - Draft restore must iterate `Object.entries` rather than passing the whole object to `form.reset({values})` — `reset` resets to defaultValues, `setFieldValue` per field is more reliable for restoring partial state
  - Auto-save effect should be skipped during draft prompt visibility (otherwise it immediately overwrites the existing draft with empty form)
  - `useRef` for the draft-saved timer prevents stale closures and allows proper cleanup on unmount
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-025
- Implemented Convex mutation hissar.update with all fields, admin role required
- Auto-sets senast_uppdaterad_av and senast_uppdaterad on every update
- Validates hissnummer uniqueness if changed, auto-adds new category values to forslagsvarden
- Implemented hissar.search query: filters by hissnummer or adress (case-insensitive), returns up to 20 results enriched with organisation name
- Created search page at /sok: debounced input (~300ms), results list with hissnummer + adress + organisation, tap navigates to /hiss/$id/redigera
- Created stub route for /hiss/$id/redigera (edit form to be implemented in US-026)
- Updated convex-api.d.ts in both admin and client apps with search and update type declarations
- Updated routeTree.gen.ts with /sok and /hiss/$id/redigera routes
- Files created: apps/admin/src/routes/_authenticated/sok.tsx, apps/admin/src/routes/_authenticated/hiss.$id.redigera.tsx
- Files modified: convex/hissar.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Convex has no built-in full-text search — use `.collect()` + JS filter in query handler, limit results to avoid large payloads
  - `hissar.update` reuses `autoAddForslagsvarden` from hissar.create — same helper works for both create and update
  - TanStack Router dynamic params: `hiss.$id.redigera.tsx` → `/hiss/:id/redigera`, `Route.useParams()` for access
  - Debounced search with "skip" sentinel: `useQuery(api.x.search, hasInput ? { search } : "skip")` skips the query when search is empty
  - Convex query results from untyped .d.ts stubs need explicit type cast: `as SearchResult[] | undefined`
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-026
- Implemented edit elevator form at /hiss/:id/redigera with diff highlighting
- Added `hissar.get` Convex query to fetch single elevator by ID
- Enhanced `checkHissnummer` query with optional `excludeId` param for edit-mode uniqueness check
- Edit form displays all fields organized in 8 sections matching creation wizard
- Diff highlighting: changed fields get amber left border + background tint via `FieldWrapper` component
- Change counter badge in header shows number of modified fields
- Save button disabled when no changes, label shows "Spara N ändringar"
- localStorage draft persistence with key `draft-hiss-{id}` — auto-save on changes, prompt on page load
- Submit calls `hissar.update` mutation, updates originalValues after save
- Success view with "Tillbaka till sök" and "Fortsätt redigera" options
- Same combobox behavior for all category fields (reuses `useSuggestions` hook pattern)
- Files created: none (edit form was stub)
- Files modified: convex/hissar.ts (added get query + enhanced checkHissnummer), apps/admin/src/routes/_authenticated/hiss.$id.redigera.tsx (full implementation), apps/admin/src/convex-api.d.ts (added hissar.get), apps/client/src/convex-api.d.ts (added hissar.get)
- **Learnings for future iterations:**
  - `serverToFormValues` helper converts Convex document to form strings (numbers → String(), undefined → "") — needed because form uses string fields for all inputs
  - `isChanged` helper compares form values vs original server values — handles both string and boolean fields
  - `FieldWrapper` component applies diff highlighting via conditional `border-l-4 border-l-amber-500 bg-amber-50/50` classes
  - `checkHissnummer` now supports `excludeId` param — when editing, the current hiss's own hissnummer is not a duplicate
  - Edit form uses single scrollable page (not wizard steps) — more natural for editing existing data
  - `ComboboxField` helper component reduces repetition for combobox fields — wraps `FieldWrapper` + `form.Field` + `useSuggestions`
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-027
- Implemented daily overview home page (field app) at / in admin app
- Added `hissar.dagensHissar` Convex query: filters by skapad_av/senast_uppdaterad_av matching current user + todayStart timestamp
- Replaced index route redirect with DailyOverview component
- Features: prominent "Ny hiss" button, "Dagens hissar" list with server data, "Sök befintlig hiss" button
- Status indicators: green checkmark for saved elevators, amber clock for drafts in localStorage
- New draft detection: scans localStorage for `draft-ny-hiss` and `draft-hiss-{id}` keys
- Edit draft detection: shows unsaved draft cards for hissar with localStorage drafts (both in today's list and standalone)
- Loading skeleton while query resolves, empty state when no activity today
- Swedish date formatting in header, responsive max-w-lg layout
- Files created: none (replaced existing stub)
- Files modified: convex/hissar.ts (added dagensHissar query), apps/admin/src/routes/_authenticated/index.tsx (full implementation), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - `dagensHissar` query accepts `todayStart` timestamp from client (respects user timezone) — compute via `new Date(y,m,d).getTime()`
  - localStorage scanning: iterate `localStorage.length` + `localStorage.key(i)` to find draft keys by prefix
  - `useState(getTodayStart)` with function initializer ensures stable timestamp across re-renders
  - Index route changed from redirect to full component — no routeTree.gen.ts changes needed since the route path remained `/`
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-028
- Implemented Convex query hissar.stats for dashboard KPIs
- Returns: totalAntal, medelAlder, moderniseringInom3Ar, totalBudgetInnevarandeAr, utanModernisering, senastInventering
- Accepts optional organisation_id for tenant isolation (admin sees all, client sees own org)
- Filters to aktiv elevators only for all calculations
- Files modified: convex/hissar.ts (added stats query), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Stats query uses `.withIndex("by_organisation_id")` when org filter provided — more efficient than `.collect()` + JS filter for org-scoped queries
  - `parseInt(rekommenderat_moderniserar, 10)` needed since the field is a string (can contain suffixes like "2030-vinga")
  - `Math.round(value * 10) / 10` for one-decimal-place rounding on averageAge
  - `moderniserar` field can be undefined or "Ej ombyggd" for elevators without modernization — check both cases
  - Budget aggregation: only include elevators where rekommenderat_moderniserar matches current year AND budget_belopp is set
  - No new codebase patterns discovered — this follows existing Convex query conventions
---

## 2026-04-02 - US-029
- Implemented dashboard page with KPI cards and Recharts visualizations at /dashboard in admin app
- Added `hissar.chartData` Convex query: aggregates 6 datasets (per district, age distribution, elevator types, top-10 manufacturers, modernization timeline, maintenance companies) — all in one query to minimize round trips
- Created shared dashboard components in packages/ui/src/components/dashboard/:
  - `kpi-cards.tsx`: KpiCards component with typed KpiItem array prop
  - `charts.tsx`: DashboardBarChart and DashboardPieChart wrappers around Recharts
- Installed Recharts 3.8.1 in packages/ui
- KPI cards show: total count, average age, modernization within 3 years, budget current year, without modernization, latest inventory date
- Charts: bar (district, age, top-10 manufacturers, modernization timeline), pie (elevator types), donut (maintenance companies)
- Admin dashboard respects org selector filter (passes organisation_id to both queries)
- Loading skeleton while data loads, empty state when no elevators
- Files created: packages/ui/src/components/dashboard/{kpi-cards.tsx, charts.tsx}
- Files modified: convex/hissar.ts, apps/admin/src/routes/_authenticated/dashboard.tsx, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts, packages/ui/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Recharts v3 Tooltip `formatter` type is strict — use `(value) => [String(value), label]` instead of annotating value as number
  - Recharts Pie `label` callback fields are optional — must handle undefined for `percent`
  - `hissar.chartData` follows same org-filter pattern as `hissar.stats` — withIndex when org provided, full collect when not
  - Shared dashboard components import from `@elevatorbud/ui/components/dashboard/charts` — wildcard export covers nested dirs
  - `as never` cast needed for org filter object passed to `useQuery` due to strict .d.ts type stubs
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-030
- Implemented client dashboard reusing shared components from packages/ui/
- Client dashboard at /dashboard uses KpiCards, DashboardBarChart, DashboardPieChart from @elevatorbud/ui/components/dashboard/
- Tenant isolation: gets organisation_id from api.users.me query, passes to hissar.stats and hissar.chartData
- No org selector — customer sees only their own org data
- Same KPI cards (6 metrics) and charts (6 visualizations) as admin dashboard
- Loading skeleton while data loads, empty state message when no elevators
- Files modified: apps/client/src/routes/_authenticated/dashboard.tsx
- **Learnings for future iterations:**
  - Client dashboard is simpler than admin — no OrgProvider context needed, just use the user's organisation_id directly
  - `useQuery(api.x.y, "skip")` used when user data hasn't loaded yet (organisation_id unknown) — prevents query with missing required filter
  - No new dependencies needed — recharts is already a dep of @elevatorbud/ui which is workspace-linked
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-031
- Implemented Convex query hissar.list with comprehensive filtering, sorting, and pagination
- Args: search (free text on hissnummer, adress, distrikt, fabrikat, hisstyp), distrikt[], hisstyp[], fabrikat[], skotselforetag[], besiktningsorgan[], byggarMin/Max, moderniserad (boolean), status (defaults to aktiv), organisation_id, sort, order, page, limit
- Pagination: returns { data, totalCount, page, limit, totalPages } — supports 25/50/100 per page
- Sorting: by any field with Swedish locale collation, undefined values pushed to end
- Tenant isolation via organisation_id (uses by_organisation_id index when provided)
- Enriches results with organisationsnamn via org cache to minimize db lookups
- Updated convex-api.d.ts in both admin and client apps with hissar.list type declaration
- Files modified: convex/hissar.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - For list queries with many filter options: use `v.optional(v.array(v.string()))` for multi-select filters, apply with Set for O(1) lookups
  - Sorting by dynamic field: cast record to `Record<string, unknown>` and compare with locale-aware `localeCompare("sv")` for strings, numeric comparison for numbers
  - Org name caching in list query: use `Map<string, string>` cache to avoid refetching same org for every elevator in page
  - Status filter defaults to "aktiv" — if not explicitly provided, only active elevators are shown
  - `moderniserad` boolean filter: true = has moderniserar value that is not "Ej ombyggd", false = no value or "Ej ombyggd"
---

## 2026-04-02 - US-032
- Implemented elevator registry page with table and filters at /register in both admin and client apps
- Admin page: TanStack Table with 9 data columns + edit action column, sortable headers, click-to-edit navigation
- Client page: same table without edit column, read-only (no navigation on row click), tenant-isolated via user's organisation_id
- Multi-select filter dropdowns for Distrikt, Hisstyp, Fabrikat using Popover + Checkbox components
- Byggår range filter with two number inputs (min/max)
- Free text search with 300ms debounce across hissnummer, adress, distrikt, fabrikat, hisstyp
- Server-side sorting via hissar.list query args (sort + order) with TanStack Table in manualSorting mode
- Server-side pagination (25/50/100 per page) with page navigation controls
- Admin org filter via OrgProvider context, client org filter via user's organisation_id
- Filter options loaded from forslagsvarden.list query (single query, grouped client-side by category)
- Installed @tanstack/react-table in client app
- Files modified: apps/admin/src/routes/_authenticated/register.tsx, apps/client/src/routes/_authenticated/register.tsx, apps/client/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - TanStack Table `manualSorting: true` + `manualPagination: true` for server-side data — table manages UI state, server handles actual sort/page
  - Multi-select filter pattern: Popover + Checkbox list + Badge count indicator — lightweight alternative to cmdk/Command
  - `forslagsvarden.list` with empty args `{}` returns all categories — group by `kategori` client-side instead of 5 separate queries
  - Server-side sort state: `SortingState` from TanStack Table → extract `id` (field) and `desc` (boolean) → pass as `sort`/`order` to query
  - Cycling sort: click once = asc, click again = desc, click again = clear — implemented via toggle function checking existing state
  - Admin register uses `window.location.href` for row click navigation (simpler than using router navigate in table row onClick)
  - Client register uses `"skip"` sentinel for query args until user's organisation_id is loaded — prevents query with missing org filter
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-033
- Implemented elevator detail view at /hiss/:id in both admin and client apps
- Admin detail view: all elevator info in 8 sections (Card components) + edit button navigating to /hiss/:id/redigera
- Client detail view: read-only, no edit button — same section layout
- Updated admin registry: row click now navigates to detail view (not edit form); edit icon button preserved for direct edit access
- Updated client registry: rows now clickable, navigating to detail view
- Detail sections: Identifiering, Teknisk specifikation, Dörrar och korg, Maskineri, Besiktning och underhåll, Modernisering, Nödtelefon, Kommentarer
- Metadata footer shows skapad_datum and senast_uppdaterad dates
- Admin header includes org name (fetched via organisationer.get query)
- Status badge (Aktiv/Rivd/Arkiverad) shown next to hissnummer
- Back button navigates to /register
- Files created: apps/admin/src/routes/_authenticated/hiss.$id.index.tsx, apps/client/src/routes/_authenticated/hiss.$id.tsx
- Files modified: apps/admin/src/routes/_authenticated/register.tsx (row click → detail), apps/client/src/routes/_authenticated/register.tsx (rows clickable), routeTree.gen.ts (x2, gitignored)
- **Learnings for future iterations:**
  - Admin app uses `hiss.$id.index.tsx` for the detail view at `/hiss/$id/` (index route) alongside existing `hiss.$id.redigera.tsx`
  - Client app uses `hiss.$id.tsx` since there's no redigera route — simpler naming
  - `organisationer.get` with requireTenantAccess works for both admin and client apps — admin can view any org, client restricted to own org
  - `DetailField` component handles all value types: undefined→"—", boolean→"Ja"/"Nej", number/string→String()
  - Budget/price formatting: `toLocaleString("sv-SE") + " kr"` for Swedish number formatting
  - No backend changes needed — `hissar.get` query already existed from US-026
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-034
- Implemented registry export (CSV and Excel) for both admin and client apps
- Created `hissar.exportData` Convex query: same filtering as `hissar.list` but returns all matching records without pagination, enriched with org name
- Refactored common filter logic into shared `filterArgs` schema and `fetchAndFilter()` helper in convex/hissar.ts
- Created `packages/utils/src/export.ts` with CSV/Excel generation utilities: `generateCSV`, `generateExcel`, `downloadCSV`, `downloadExcel`
- Installed xlsx 0.18.5 in packages/utils for Excel generation (SheetJS)
- Added `@elevatorbud/utils` workspace dependency to admin and client apps
- Export button (Popover dropdown with CSV and Excel options) added to registry page header in both apps
- Lazy-loading pattern: export data query uses "skip" sentinel until user clicks export, then fetches all matching records
- Exports respect current filter state (search, multi-select filters, range filters, org isolation)
- Tenant isolation: client exports automatically scoped to user's organisation_id
- CSV includes BOM for correct UTF-8 detection in Excel
- Excel export includes auto-sized columns and Swedish headers for all 41 elevator fields
- Files created: packages/utils/src/export.ts
- Files modified: convex/hissar.ts (refactored filters + added exportData query), apps/admin/src/routes/_authenticated/register.tsx (export UI), apps/client/src/routes/_authenticated/register.tsx (export UI), apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts, packages/utils/package.json, apps/admin/package.json, apps/client/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - TypeScript 6 `Uint8Array` is not assignable to `BlobPart` — use `ArrayBuffer` return type from XLSX.write instead
  - xlsx `XLSX.write(wb, { type: "array" })` returns ArrayBuffer (not Uint8Array) — compatible with Blob constructor
  - CSV BOM (`\uFEFF`) is essential for Swedish characters (å, ä, ö) to display correctly when opening in Excel
  - Lazy export pattern: set state to trigger query, useEffect watches for data arrival + format state, triggers download, clears state
  - Refactoring shared filter logic into helper function avoids code duplication between list and export queries
  - `db.get()` in Convex with generated types returns a union of all document types — need explicit cast when you know the table
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-035
- Implemented two new Convex queries for modernization planning views
- `hissar.moderniseringTidslinje`: counts elevators per recommended modernization year, sorted chronologically. Returns `{ ar, antal }[]`
- `hissar.moderniseringBudget`: aggregates budget in three dimensions — per year (`perAr`), per district (`perDistrikt`), per elevator type (`perTyp`). Returns `{ perAr: { ar, belopp }[], perDistrikt: { namn, belopp }[], perTyp: { namn, belopp }[] }`
- Both queries support optional `organisation_id` filter for tenant isolation (uses `by_organisation_id` index when provided)
- Both queries filter to `status === "aktiv"` only
- Updated convex-api.d.ts in both admin and client apps with new query type declarations
- Files changed: convex/hissar.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Both queries follow the same org-filter + aktiv-filter pattern as `stats` and `chartData` — consider extracting a shared helper if more queries need this
  - `moderniseringTidslinje` is a more focused version of the timeline data in `chartData` — the page-specific query returns `ar`/`antal` instead of `name`/`count` for clearer semantics
  - `moderniseringBudget` only counts elevators with BOTH `rekommenderat_moderniserar` AND `budget_belopp` set — elevators missing either are excluded from budget calculations
  - `parseInt(rekommenderat_moderniserar, 10)` handles string values like "2030-vinga" correctly — parseInt stops at the first non-numeric character
  - No new codebase patterns discovered — this follows existing Convex query conventions
---

## 2026-04-02 - US-036
- Implemented modernization planning page at /modernisering in both admin and client apps
- Added two new Convex queries: `moderniseringPrioritetslista` (elevator list sorted by recommended year, filterable by year range) and `moderniseringAtgarder` (count per modernization action)
- Timeline view: color-coded bar chart with per-year elevator counts (red ≤1yr, orange 2-4yr, yellow 5-9yr, green 10+yr)
- Clickable period summary cards filter the priority list
- Budget overview: 3 charts (per year with cumulative curve as LineChart, per district bar chart, per type bar chart) + total budget display
- Priority list: table sorted by recommended year with urgency badges, hissnummer, adress, distrikt, budget, åtgärd columns — responsive with hidden columns on small screens
- Action summary: list of most common modernization actions with counts
- Admin app uses org selector filter, client app uses tenant isolation via user's organisation_id
- Installed `recharts` as direct dependency in both admin and client apps
- Files created: none (replaced existing stubs)
- Files modified: convex/hissar.ts (added 2 queries), apps/admin/src/routes/_authenticated/modernisering.tsx, apps/client/src/routes/_authenticated/modernisering.tsx, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts, apps/admin/package.json, apps/client/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Apps using Recharts directly (not via shared UI components) must have `recharts` as a direct dependency — workspace linking doesn't make it transitively available
  - Recharts Tooltip `formatter` must NOT have explicit type annotations on `value`/`name` params — use `(value) =>` not `(value: number) =>` to match `ValueType | undefined`
  - TanStack Router typed `<Link>` with dynamic params like `/hiss/$id` can cause type errors — use plain `<a href={...}>` for simpler navigation from data tables
  - Recharts `<LineChart>` can contain both `<Bar>` and `<Line>` for combined visualizations (bar + cumulative line)
  - Priority list query accepts optional `arFran`/`arTill` for year-range filtering — avoids re-fetching when user clicks period cards
  - Period cards with `selectedPeriod` state provide a natural filter UX for the priority list below
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-037
- Implemented three Convex queries for maintenance and inspection views
- `hissar.besiktningskalender`: counts elevators per inspection month (returns all 12 months with counts, preserving month order)
- `hissar.skotselforetag`: counts per maintenance company + matrix of company × district with Swedish locale sorting
- `hissar.nodtelefonstatus`: with/without emergency phone counts, upgrade needs count, total upgrade cost, all broken down per district
- All three queries support optional `organisation_id` filter for tenant isolation (uses `by_organisation_id` index when provided)
- All three filter to `status === "aktiv"` only
- Updated convex-api.d.ts in both admin and client apps with new query type declarations
- Files changed: convex/hissar.ts, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - `besiktningskalender` pre-initializes all 12 months to 0 so UI always gets a complete monthly dataset (no gaps)
  - `skotselforetag` returns both a flat `foretag` list (sorted by count desc) and a `distrikt` array for matrix column headers
  - Each foretag entry has `perDistrikt` array matching the `distrikt` order — ready for table rendering
  - `nodtelefonstatus` aggregates at both global and per-district level — efficient since it's a single pass over aktiva
  - These queries follow the same org-filter + aktiv-filter pattern as stats/chartData — no new patterns discovered
---

## 2026-04-02 - US-038
- Implemented maintenance and inspection page at /underhall in both admin and client apps
- Added `hissar.besiktningslista` Convex query: returns elevators for a selected inspection month, enriched with org name
- Section 1 — Besiktningskalender: 3 KPI cards (this month, yearly total, monthly average), bar chart with clickable months (current month highlighted in blue, selected in red), elevator list table for selected month
- Section 2 — Skötselföretag: horizontal bar chart showing count per maintenance company, company × district matrix table with sticky first column
- Section 3 — Nödtelefonstatus: 4 KPI cards (with/without, needs upgrade, total cost), per-district table with colored badges and totals row
- Admin version uses org selector filter from OrgProvider context
- Client version uses tenant isolation via user's organisation_id (from api.users.me)
- Files created: none (replaced existing stubs)
- Files modified: convex/hissar.ts (added besiktningslista query), apps/admin/src/routes/_authenticated/underhall.tsx, apps/client/src/routes/_authenticated/underhall.tsx, apps/admin/src/convex-api.d.ts, apps/client/src/convex-api.d.ts
- **Learnings for future iterations:**
  - Recharts BarChart `onClick` handler parameter is typed as `MouseHandlerDataParam` which doesn't include `activePayload` — use `(data: any)` to avoid TS errors
  - Recharts `<Cell>` component inside `<Bar>` allows per-bar coloring and opacity — useful for highlighting selected/current items
  - `layout="vertical"` on BarChart rotates the chart for horizontal bars — useful for company names that need more space
  - Sticky column in scrollable table: `sticky left-0 bg-background` on TableHead and TableCell for the first column
  - Client underhall page uses same "skip" sentinel pattern as other client pages: `useQuery(api.x, user?.org_id ? {org: user.org_id} : "skip")`
  - Manual browser verification needed (no browser testing tools configured)
---

## 2026-04-02 - US-039
- Implemented Excel parser for Hissar sheet with compound fields in packages/utils/src/excel-import.ts
- Column mapping: 34 columns (A-AH) with standard Hisskompetens Excel format
- Mandatory columns validated: C (hissnummer), J (byggar), L (plan/dorrar), AB (moderniserar)
- Compound field parsers: marklast (stored as string), plan/dorrar (split to antal_plan + antal_dorrar as numbers), korgstorlek (string), dagoppning (string), nodtelefon (parsed to 5 fields)
- Special value handling: byggar handles "Okant"/"Okänt" → undefined; moderniserar keeps "Ej ombyggd" and suffixed years as-is
- Budget parser strips currency markers ("kr", "sek") and whitespace
- Boolean parser handles Swedish (Ja/Nej) and English (Yes/No) variants
- Updated packages/utils/src/index.ts with real exports replacing placeholder
- Exported HISSAR_COLUMNS and ColumnDef for reuse by US-040 (Nodtelefoner/Rivna hissar parsers)
- Files created: packages/utils/src/excel-import.ts
- Files modified: packages/utils/src/index.ts
- **Learnings for future iterations:**
  - xlsx library `sheet_to_json` with `{ header: 1, raw: false }` returns string[][] for positional column access
  - `raw: false` ensures all values are strings (not auto-parsed numbers) — needed for compound field parsing
  - HISSAR_COLUMNS is exported for reuse by the Rivna hissar parser (US-040) which has same structure minus column AH
  - ParsedHiss type includes `_source_row` and `_source_sheet` metadata for error reporting during import
  - `_organisation_namn` field stores the raw org name from Excel column A — used for org matching during import (not stored in DB)
  - validateWorkbookSheets() checks for required "Hissar" sheet and optional "Nodtelefoner"/"Rivna hissar" sheets
---

## 2026-04-02 - US-040
- Implemented Excel parsers for Nodtelefoner and Rivna hissar sheets in packages/utils/src/excel-import.ts
- Added `status` field to ParsedHiss type ('aktiv' | 'rivd' | 'arkiverad') for import-time status assignment
- `parseNodtelefonerSheet()`: parses Nodtelefoner sheet with column G as hissnummer join key, extracts har_nodtelefon, modell, typ, behover_uppgradering, pris
- `parseRivnaHissarSheet()`: uses RIVNA_HISSAR_COLUMNS (HISSAR_COLUMNS minus column AH), no headers (data from row 1), sets status='rivd' on all records
- `joinNodtelefoner()`: merges nodtelefon data into Hissar elevators by hissnummer (case-insensitive), uses case-insensitive district matching when multiple elevators share same hissnummer
- `parseExcelImport()`: orchestrates all three parsers, joins Nodtelefoner into Hissar, combines Hissar + Rivna results, returns FullImportResult with per-sheet metadata
- Exported new functions and types: parseNodtelefonerSheet, parseRivnaHissarSheet, parseExcelImport, NodtelefonerParseResult, FullImportResult
- Files modified: packages/utils/src/excel-import.ts, packages/utils/src/index.ts
- **Learnings for future iterations:**
  - Rivna hissar reuses HISSAR_COLUMNS with `.filter(col => col.letter !== "AH")` — no need to duplicate the column mapping
  - Nodtelefoner sheet column G (index 6) is the join key — other columns hold expanded emergency phone fields
  - `joinNodtelefoner` builds a Map<lowercaseHissnummer, ParsedHiss[]> for O(1) lookup, handles ambiguity with case-insensitive district matching
  - Rivna hissar has NO column headers — data starts at row 1 (0-indexed row 0), unlike Hissar which has headers at row 2
  - `parseRivnaRow` duplicates the switch/case parsing logic from `parseHissarRow` — could be refactored into a shared helper but kept separate for clarity since Rivna excludes compound_nodtelefon
  - `FullImportResult.sheets` object tracks found/count/joined metadata per sheet — useful for import preview UI (US-041)
---
