# Ralph Progress Log
Started: tors  2 apr. 2026 21:22:55 CEST
---

## Codebase Patterns
- TanStack Start with file-based routing: routes in `apps/admin/src/routes/_authenticated/`
- Route tree auto-generated in `routeTree.gen.ts` (gitignored) — regenerated by dev server / vite build
- Sidebar nav defined as `const xxxNav = [...] as const` arrays in `app-sidebar.tsx`, with `SidebarGroup > SidebarGroupLabel > SidebarGroupContent > SidebarMenu` structure
- Icons from `lucide-react`
- Active state: `pathname.startsWith(item.href)` for grouped nav, exact match for main nav
- Typecheck: `npx tsc --noEmit --project apps/admin/tsconfig.json` from repo root
- shadcn/ui components imported from `@elevatorbud/ui/components/ui/*`
- Convex API imported as `import { api } from "@convex/_generated/api"`
- When a route has `validateSearch`, all `<Link to=...>` pointing to it must include `search` prop — TanStack Router enforces this at type level
- shadcn components installed via `pnpm dlx shadcn@latest add <name>` from `packages/ui/` directory
- Tabs component: `@elevatorbud/ui/components/ui/tabs` — Tabs, TabsList, TabsTrigger, TabsContent

## 2026-04-02 - US-001
- Added 'Webbplats' sidebar group with Globe icon between mainNav and adminNav groups
- Created placeholder route at `apps/admin/src/routes/_authenticated/webbplats.tsx`
- Updated `routeTree.gen.ts` (auto-generated, gitignored) to register the new route
- Files changed:
  - `apps/admin/src/components/app-sidebar.tsx` (modified)
  - `apps/admin/src/routes/_authenticated/webbplats.tsx` (created)
  - `apps/admin/src/routeTree.gen.ts` (modified, gitignored)
- **Learnings for future iterations:**
  - `routeTree.gen.ts` is gitignored — it is regenerated by the dev server automatically, but you must update it locally for typecheck to pass during development
  - The sidebar follows a consistent pattern: define a nav array, then render a `SidebarGroup` with `SidebarGroupLabel`
  - TanStack Router strictly types `Link to=` props — routes must exist in `routeTree.gen.ts` for typecheck
---

## 2026-04-02 - US-002
- Installed shadcn tabs component in `packages/ui`
- Rewrote webbplats route with 4 tabs (Startsida, Om oss, Tjänster, Kontakt)
- Each tab uses `useQuery(api.cms.getPage, { slug })` to load page data
- Active tab persists via `?sida=` URL search param using TanStack Router `validateSearch`
- Updated sidebar Link to include required `search` prop
- Files changed:
  - `apps/admin/src/routes/_authenticated/webbplats.tsx` (rewritten)
  - `apps/admin/src/components/app-sidebar.tsx` (modified — added search prop to webbplats Link)
  - `packages/ui/src/components/ui/tabs.tsx` (created by shadcn)
- **Learnings for future iterations:**
  - Adding `validateSearch` to a route makes `search` prop required on all `<Link>` pointing to it — must update sidebar Link too
  - Convex `useQuery` returns `undefined` while loading, `null` if document doesn't exist — use this to distinguish loading vs empty state
  - The tabs component uses Radix UI primitives via `radix-ui` package (not `@radix-ui/react-tabs`)
---
