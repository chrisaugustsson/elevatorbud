# Ralph Progress Log
Started: fre  3 apr. 2026 12:21:09 CEST
---

## Codebase Patterns
- Unicode in JSX: Source files had `\uXXXX` escape sequences in JSX text content and string attributes. These are NOT decoded in JSX text — must use actual UTF-8 characters instead.
- Feature components live in `apps/*/src/features/<domain>/components/`
- Both admin and client apps share similar patterns — changes to shared domain labels may need updating in both apps
- TanStack Router: use `useRouter()` + `router.history.back()` for back navigation instead of hardcoded `<Link>` paths
- Debounce pattern: use `useEffect` with `setTimeout`/`clearTimeout` for debouncing search inputs that drive Convex queries — clear immediately when input is empty for instant feedback
- Sidebar state: use `useSidebar()` hook from `@elevatorbud/ui/components/ui/sidebar` — provides `state` ("expanded"|"collapsed"), `toggleSidebar()`, `isMobile`
- Collapsed sidebar: use `group-data-[collapsible=icon]:hidden` to hide elements when sidebar is in icon-only mode, `group-data-[collapsible=icon]:ml-0` to adjust spacing
- TanStack Form: `useForm` return type is too complex for `ReturnType<typeof useForm<...>>` — inline form fields in each dialog component instead of extracting shared form components
- Table actions column: use `columnHelper.display({ id: "actions", ... })` for non-data columns (no accessor)
- Confirmation dialogs: use Dialog (not AlertDialog — doesn't exist in UI package) with destructive Button variant for dangerous actions
- Current user identity: `useUser()` from `@elevatorbud/auth` gives `user.id` (Clerk user ID) — compare with `clerk_user_id` field in user records
- DropdownMenu: available at `@elevatorbud/ui/components/ui/dropdown-menu` — use DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator
- Convex optional field clearing: `v.optional()` args with `if (field !== undefined)` pattern means `undefined` = "don't change". To clear a field, add explicit logic (e.g., clear org when role is admin) rather than relying on `undefined`

---

## 2026-04-03 - US-001
- Replaced all `\uXXXX` Unicode escape sequences with actual UTF-8 Swedish characters (å, ä, ö, Å, Ä, Ö, é)
- Root cause: Source files contained JavaScript Unicode escape sequences that render as literal text in JSX content
- Files changed: 24 files across admin and client apps (151 replacements total)
  - `apps/admin/src/features/elevator/components/` — modernization, emergency phone, doors, technical specs, inspection, machinery, basic info, comments, wizard, wizard steps
  - `apps/admin/src/features/register/components/register-table.tsx`
  - `apps/admin/src/routes/_authenticated/` — hiss.$id.redigera.tsx, ny.tsx, hiss.$id.index.tsx
  - `apps/client/src/features/modernization/components/` — priority-list, measures-card, budget-overview, timeline-chart, urgency-helpers
  - `apps/client/src/features/register/components/` — register-table, register-pagination, register-filters
  - `apps/client/src/routes/_authenticated/hiss.$id.tsx`
- **Learnings for future iterations:**
  - The issue was NOT in the data layer (Convex) or import pipeline — it was in the JSX source files
  - `\uXXXX` in JSX text content is treated as literal characters, not Unicode escapes
  - Used Python script with regex for bulk replacement across many files
  - Always check both admin AND client apps for shared domain terms
---

## 2026-04-03 - US-002
- Fixed back button navigation from elevator edit page — removed hardcoded `/sok` links
- Header back button and Cancel button now use `router.history.back()` so they return to wherever the user came from
- Success confirmation "Tillbaka" button now links to `/hiss/$id` (elevator detail page) instead of `/sok`
- Files changed:
  - `apps/admin/src/routes/_authenticated/hiss.$id.redigera.tsx` — added `useRouter`, replaced 3 hardcoded `/sok` links
- **Learnings for future iterations:**
  - TanStack Router provides `useRouter()` hook with `router.history.back()` for browser-like back navigation
  - `Link` component supports `params` prop for dynamic route segments: `<Link to="/hiss/$id" params={{ id }}>`
---

## 2026-04-03 - US-003
- Added debounced search on the users page (300ms delay)
- Introduced `debouncedSearch` state that lags behind `searchText` by 300ms
- Clearing the search field immediately resets results (no delay for empty string)
- The Convex `useQuery` now uses `debouncedSearch` instead of `searchText`
- Files changed:
  - `apps/admin/src/routes/_authenticated/admin.anvandare.tsx` — added `debouncedSearch` state + useEffect debounce logic, updated useQuery param
- **Learnings for future iterations:**
  - Simple debounce with useEffect + setTimeout is sufficient — no need for external libraries
  - Important to handle empty string as immediate clear for good UX
---

## 2026-04-03 - US-004
- Moved sidebar toggle button from header layout into the sidebar component
- Toggle button placed in SidebarHeader next to the "Hisskompetens" branding
- Uses ChevronsLeft/ChevronsRight icons to indicate expand/collapse direction
- Visible in both expanded (right-aligned) and collapsed (centered) states
- Removed SidebarTrigger and Separator imports from _authenticated.tsx header
- Files changed:
  - `apps/admin/src/shared/components/app-sidebar.tsx` — added toggle button with useSidebar hook, ChevronsLeft/ChevronsRight icons, Button component
  - `apps/admin/src/routes/_authenticated.tsx` — removed SidebarTrigger, Separator imports and usage from header
- **Learnings for future iterations:**
  - `group-data-[collapsible=icon]:ml-0` overrides `ml-auto` in collapsed state so the button centers properly
  - `shrink-0` on the button prevents it from being squished in collapsed mode
  - Swedish sr-only text: "Fäll ihop sidopanel" / "Expandera sidopanel"
---

## 2026-04-03 - US-005
- Added user name and email display in expanded sidebar footer
- Used Clerk's `useUser()` hook to get user data (avoids extra Convex query)
- Name and email shown next to UserButton avatar when sidebar is expanded
- Text hidden in collapsed state using `group-data-[collapsible=icon]:hidden`
- Both name and email truncate with `truncate` class for long values
- Files changed:
  - `apps/admin/src/shared/components/app-sidebar.tsx` — added `useUser` import, user data display in SidebarFooter
- **Learnings for future iterations:**
  - `useUser()` from Clerk gives `user.fullName` and `user.primaryEmailAddress.emailAddress`
  - Prefer Clerk hooks for display-only user info to avoid duplicate Convex queries
  - `min-w-0` on the text container is needed for `truncate` to work inside flex containers
---

## 2026-04-03 - US-006
- Added edit user dialog with pencil icon button in each user row
- Edit dialog pre-fills with current user data (name, email, role, organization)
- Changing role to 'admin' clears the organization field automatically
- Changing role to 'customer' requires selecting an organization
- Validation: required name, email format, org required for customer role
- Calls `api.userAdmin.update` action on save
- Files changed:
  - `apps/admin/src/routes/_authenticated/admin.anvandare.tsx` — added Pencil icon import, `editingUser` state, actions column with edit button, `EditUserDialog` and `EditUserDialogInner` components
- **Learnings for future iterations:**
  - TanStack Form's `useForm` return type is too complex for `ReturnType<typeof useForm<...>>` — just inline form fields in each dialog rather than trying to extract a shared form component
  - The `columnHelper.display()` method is used for non-data columns like actions (no accessor needed)
  - `useAction(api.userAdmin.update)` was already imported in the component — reuse existing imports
---

## 2026-04-03 - US-007
- Added dropdown menu (MoreHorizontal icon) to each user row with Deactivate/Activate and Delete options
- Deactivate toggles active state with confirmation dialog — calls `api.userAdmin.deactivate` or new `api.userAdmin.activate`
- Deactivated users shown with reduced opacity (opacity-50 on table row) + existing "Inaktiv" badge
- Delete shows confirmation dialog, calls `api.userAdmin.remove`
- Cannot delete yourself — button disabled and labeled "Kan inte ta bort dig själv" when `clerk_user_id` matches current Clerk user
- Created new backend: `api.userAdmin.activate` action + `userAdminInternal.activateUser` mutation (sets active=true, unbans via Clerk)
- Files changed:
  - `apps/admin/src/routes/_authenticated/admin.anvandare.tsx` — DropdownMenu imports, useUser import, deactivate/activate/remove actions, confirmation dialogs, actions column dropdown
  - `convex/userAdmin.ts` — added `activate` action, updated `internalRef` type
  - `convex/userAdminInternal.ts` — added `activateUser` internal mutation
  - `apps/admin/src/convex-api.d.ts` — added `activate` and `activateUser` types
  - `apps/client/src/convex-api.d.ts` — added `activate` and `activateUser` types
- **Learnings for future iterations:**
  - Backend had `deactivate` but no `activate` — needed to create the counterpart for toggle behavior
  - `useUser()` from `@elevatorbud/auth` provides `user.id` which is the Clerk user ID, matching `clerk_user_id` in the database
  - DropdownMenu component available at `@elevatorbud/ui/components/ui/dropdown-menu`
  - No AlertDialog in the UI package — use regular Dialog components for confirmation flows
---

## 2026-04-03 - US-008
- Fixed org-clearing bug when changing user role from customer to admin
- Backend `updateUser` mutation now explicitly clears `organization_id` when role is set to "admin"
- Clerk metadata update also clears org for admin role
- All other US-008 acceptance criteria were already met by US-006 (edit dialog org dropdown, role/org validation, org filter on users list)
- Files changed:
  - `convex/userAdminInternal.ts` — updateUser mutation clears org_id when role is admin
  - `convex/userAdmin.ts` — update action Clerk metadata clears org_id for admin role
- **Learnings for future iterations:**
  - When Convex mutation uses `if (field !== undefined)` pattern for optional patches, `undefined` means "don't change" not "clear" — need explicit clearing logic for dependent field clearing
  - Role/org business rule (admin = no org) should be enforced in backend, not just frontend
---

## 2026-04-03 - US-009
- Replaced card-based list with React Table (same pattern as users page)
- Added columns: Value (sortable), Status (active/inactive badge), Actions (dropdown menu with Edit, Merge, Deactivate/Activate)
- Inactive items shown with reduced opacity (opacity-50 on table row)
- Category dropdown label changed from "Category" to "Kategori" (Swedish)
- Added `activate` mutation to `convex/suggestedValues.ts` (only `deactivate` existed)
- Actions moved from inline icon buttons to DropdownMenu with MoreHorizontal trigger (consistent with users page)
- Search filter, 'Nytt värde' button, active/inactive counts all preserved
- Files changed:
  - `apps/admin/src/routes/_authenticated/admin.referensdata.tsx` — replaced cards with table, added activate mutation usage, dropdown menu actions
  - `convex/suggestedValues.ts` — added `activate` mutation
  - `apps/admin/src/convex-api.d.ts` — added `activate` type for suggestedValues
  - `apps/client/src/convex-api.d.ts` — added `activate` type for suggestedValues
- **Learnings for future iterations:**
  - React Table pattern is consistent across pages (users, reference data) — use `createColumnHelper`, `useReactTable`, `getCoreRowModel`, `getSortedRowModel`
  - DropdownMenu pattern is the standard for row actions across the admin app
  - When adding toggle behavior (activate/deactivate), always check if both mutations exist in backend
---
